<h1>Listing Users</h1>

<table class="table">
  <%= for user <- @users do %>
    <tr>
      <%= render "user.html", user: user %>
      <td> <%= link "View #{user.name}", to: user_path(@conn, :show, user.id) %> </td>
    </tr>
  <% end %>
</table>